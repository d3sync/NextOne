<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="NextOne.Views.NewItemPage"
             Title="Connect to Server">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Disconnect" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Connect" Clicked="Socket_Handler" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Server Address" FontSize="Medium" />
            <Entry Text="{Binding Item.Text}" d:Text="192.168.0.4" FontSize="Large" Keyboard="Telephone" x:Name="ipAddress"/>
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
